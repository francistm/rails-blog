<% provide(:page_title, @post.title) %>

<article>
  <h1 class="page-title"><%= @post.title %></h1>
  <div class="page-meta text-right"><%= l @post.published_at %></div>
  <div class="content markdown"><%= sanitize parse_markdown @post.content %></div>
</article>
