<tr>
  <td><%= link.id %></td>
  <td><%= link.title %></td>
  <td><%= link_to link.url %></td>
  <td>
    <%= link_to '编辑', [:edit, :admins, link], class: 'btn btn-default' %>
    <%= link_to '删除', [:admins, link], method: :delete, class: 'btn btn-danger' %>
  </td>
</tr>
